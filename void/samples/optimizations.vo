test = (a) => {
    return 1;
    b = ((1+1)*2)/2 + 10 * a;
    b = ((1+1)*2)/2 + 10 * a;
    b = ((1+1)*2)/2 + 10 * a;
    b = ((1+1)*2)/2 + 10 * a;
    b = ((1+1)*2)/2 + 10 * a;
};
a = 500000;
while (a > 0) => {
    test(a);
    a = a - 1;
}